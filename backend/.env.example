# Copy to .env for local overrides (docker-compose reads this automatically).
# Uncomment/set DATABASE_URL to point at Postgres instead of the SQLite default.

# DATABASE_URL=postgresql+psycopg2://forgeml:forgeml@localhost:5432/forgeml
# CORS_ORIGINS=http://localhost:5173
